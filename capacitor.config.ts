import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tABU',
  appName: 'Tabu Pro',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
